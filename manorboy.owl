def C(var i) {
    return &() -> i;
}

def A(var k, var x1, var x2, var x3, var x4, var x5) {
    def B() {
        println "inside B.";
        k := k-1;
        return A(k, B, x1, x2, x3, x4);
    }
    println "K: " + k;
    if (k <= 0) {
        return x4()+x5();
    } else {
        return B();
    }
}

let i := 0;
while (i <= 3) {
    println "Man or Boy? " + i;
    println A(i, C(1), C(-1), C(-1), C(1), C(0));
    i := i + 1;
}